What is JSX, and why is it used?
ans: jsx means javascript xml. it allows to write html code inside javascript.

What is the difference between State and Props?
ans: state is used to make dynamic data, props is used to pass data
What is the useState hook, and how does it work?
ans: it is used to store and update 
How can you share state between components in React?

How is event handling done in React?
ans:we have to use camelCase and need to add a event handler